{
  "title": "PCI Framework: Enterprise Input Extractor",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Root website URL to begin crawling.",
      "editor": "textfield"
    },
    "maxDepth": {
      "title": "Maximum Crawl Depth",
      "type": "integer",
      "description": "Maximum link depth to crawl from the start URL.",
      "default": 1,
      "minimum": 0
    },
    "categoriesToInclude": {
      "title": "Categories To Include",
      "type": "array",
      "description": "Only include FAQs whose questions contain these keywords. Leave empty to include all.",
      "items": {
        "type": "string"
      },
      "editor": "stringList"
    }
  },
  "required": ["startUrl"]
}
